  <%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <h5><%= f.label :event_company %><small> name of a company that hosts the event. </small></h5>
    <%= f.text_field :event_company, class: "form-control",autofocus: true %>
  </div>
  <div class="form-group">
    <h5><%= f.label :event_title %><small> name of a company that hosts the event. </small></h5>
    <%= f.text_field :event_title, class: "form-control",autofocus: true %>
  </div>
  <div class="form-group">
    <h5><%= f.label :event_type %><small> type of an event. </small></h5>
    <%= f.select :event_type, Event::ALLOWED_TYPES, class: "form-control",autofocus: true %>
  </div>
  <div class="form-group">
    <h5><%= f.label :event_info %><small> brief information on event information. </small></h5>
    <%= f.text_area :event_info, class: "form-control",autofocus: true %>
  </div>
  <div class="form-group">
    <h5><%= f.label :event_date %><small> choose event date. </small></h5>
    <%= f.datetime_select :event_date %>
  </div>
  <br></br>
  <div class="actions">
    <%= button_to "Create Event", create_event_path, class: "btn btn-primary"%>
  </div>
<% end %>
